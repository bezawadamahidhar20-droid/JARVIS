# JARVIS — runtime dependencies
# Python 3.10+  |  Windows (primary)
#
# Install:  pip install -r requirements.txt

SpeechRecognition>=3.10.0
pyttsx3>=2.90
requests>=2.31.0
python-dotenv>=1.0.0
pyautogui>=0.9.54
pillow>=10.0.0

# Used as the microphone audio backend when PyAudio has no wheel for your
# Python version (common on 3.13+ Windows). Ships its own PortAudio DLLs.
sounddevice>=0.5.0