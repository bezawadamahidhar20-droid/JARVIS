# JARVIS — pinned runtime dependencies
# Python 3.10+  |  Windows x64 (primary), Linux/macOS (diagnostics)
#
# Install:  pip install -r requirements.txt
# Setup  :  .\setup.ps1   (Windows)   |   bash setup.sh   (Linux/macOS/WSL)
#
# Version rationale
# ─────────────────
# sounddevice 0.5.5   — latest stable; ships bundled PortAudio for Windows
# numpy 2.2.3         — latest stable; required by sounddevice & faster-whisper
# faster-whisper 1.1.1— latest stable supporting Python 3.9+; pins ctranslate2≥4
# piper-tts 1.2.0     — latest stable GPL release; ONNX neural TTS
# requests 2.32.3     — latest stable; Ollama /api/generate HTTP client
# scipy 1.14.1        — diagnostic/test scripts only (not imported by main pipeline)
# ollama 0.6.2        — official Python SDK; test_qwen.py only

# ── Core runtime ──────────────────────────────────────────────────────────────
sounddevice==0.5.5
numpy==2.2.3
faster-whisper==1.1.1
piper-tts==1.2.0
requests==2.32.3

# ── Diagnostic / test scripts only ───────────────────────────────────────────
# (not imported anywhere in the main pipeline: main.py, ai/, audio/, commands/,
#  speech/, utils/ — safe to omit if you only want the assistant itself)
scipy==1.14.1
ollama==0.6.2
